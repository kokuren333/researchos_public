# Research Project MVP

`ResearchProject` is an optional workflow entity. Its `project.yaml` is JSON-compatible YAML so the dependency-free validator can read it without introducing a YAML runtime. Analysis or later statuses require question, protocol, and analysis-plan files. Results require an analysis plan.
