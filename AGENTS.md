# Research OS Public contributor guide

- Keep reusable infrastructure in `core/`; keep public subject matter in `domains/` and `meta/`.
- Evidence must reference a Source and a locator.
- AI-generated prose is never Evidence.
- Do not add private research material, credentials, copyrighted local files, or restricted datasets.
- Run `npm test` before committing.
