import { readFile } from "node:fs/promises";import path from "node:path";import { fileURLToPath } from "node:url";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const c=JSON.parse(await readFile(path.join(root,"domains/concept-contracts/measurement.validity.json"),"utf8"));if(c.required_claims.length<6||c.claim_evidence.length<2||Object.keys(c.examples).length!==4||!c.quality_gate.learner_can_transfer.length)throw new Error("Concept Contract incomplete");console.log("measurement.validity concept contract passed");
