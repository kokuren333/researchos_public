import { readFile } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),".."); const input=JSON.parse(await readFile(path.join(root,"tests/fixtures/choose-statistical-model.valid.json"),"utf8")); for(const key of ["research_question","outcome","data_description","purpose"])if(!input[key])throw new Error(`missing input: ${key}`);
const output={recommended_model:{family:"linear regression",specification:"outcome change ~ exposure + pre-specified covariates",rationale:"連続アウトカムと一人一観測という情報には整合するが、残差構造などの仮定確認が必要。"},alternatives:[{model:"robust regression",when_preferred:"外れ値や裾の重い残差が重要な場合",reason_not_primary:"分布と感度分析の情報が不足"}],assumptions:{required:["アウトカムの尺度が連続である","観測単位が独立である"],uncertain:["線形性","残差分散","未測定交絡"]},data_requirements:{satisfied:["outcome","exposure"],missing:["欠測パターン","サンプルサイズ","分布診断"]},diagnostics:{required:["残差診断","影響点","感度分析"]},limitations:["モデル選択は因果識別を保証しない","変数選択の判断は別途必要"],missing_information:["sample_size","missingness","repeated_measures confirmation"],decision_status:"conditional"}; if(!output.recommended_model.family||!output.alternatives.length||!output.assumptions.required.length||!output.diagnostics.required.length||!output.missing_information.length)throw new Error("incomplete model decision"); if(!["recommended","conditional","insufficient-information"].includes(output.decision_status))throw new Error("invalid decision status"); console.log("choose-statistical-model execution passed");
