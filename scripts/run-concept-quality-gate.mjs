import { readFile, writeFile, mkdir } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const c=JSON.parse(await readFile(path.join(root,"domains/priority-validity.json"),"utf8"));const quality=JSON.parse(await readFile(path.join(root,"dist/generated/concept-quality-audit.json"),"utf8"));const grounding=c.source&&c.locator?"pass":"revise";const result={concept:c.id,writer:"source-data",deepener:"rendered",quality:quality.verdict,grounding,gate:quality.verdict==="pass"&&grounding==="pass"?"READY_FOR_HUMAN_REVIEW":"REVISE",human_review_required:true};await mkdir(path.join(root,"dist/generated"),{recursive:true});await writeFile(path.join(root,"dist/generated/concept-quality-gate.json"),JSON.stringify(result,null,2),"utf8");if(result.gate==="REVISE")throw new Error("concept quality gate requires revision");console.log("concept quality gate: ready for human review");
