import { readFile, writeFile, mkdir } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const c=await import("./lib/load-concept-content.mjs").then(m=>m.loadConceptContent(root,"measurement.validity"));const issues=[];if(c.definition.length<40)issues.push("definition is too thin");if(c.intuition.length<40)issues.push("intuition is too thin");for(const topic of ["validation","population","error","proxy"])if(!Object.values(c).some(v=>String(v).toLowerCase().includes(topic)))issues.push(`${topic} is missing`);const result={concept:c.id,verdict:issues.length?"revise":"pass",issues,missing_depth:issues,strong_points:["Source locator exists","Concept-specific source data is used"],overall_assessment:issues.length?"内容密度に改善が必要。":"本文は構造・内容監査を通過し、人間確認待ち。"};await mkdir(path.join(root,"dist/generated"),{recursive:true});await writeFile(path.join(root,"dist/generated/concept-quality-audit.json"),JSON.stringify(result,null,2),"utf8");console.log(`concept quality audit: ${result.verdict}`);
