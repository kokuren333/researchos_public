import { readFile } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),".."); const input=JSON.parse(await readFile(path.join(root,"tests/fixtures/assess-measurement-validity.valid.json"),"utf8"));
for(const key of ["construct","operationalization"]) if(!input[key]) throw new Error(`missing input: ${key}`);
const output={construct:{stated:input.construct},operationalization:{stated:input.operationalization},assessment:{construct_validity:{status:"unknown",rationale:"構成概念を一問の自己報告がどこまで代表するかは情報不足。"},content_validity:{status:"partially-supported",rationale:"提示された操作化は運動頻度の一部を捉えるが習慣全体を網羅しない。"},criterion_validity:{status:"not-assessable",rationale:"reference standardとvalidation studyが報告されていない。"}},measurement_error:{likely_sources:["recall","social desirability"],direction:"mixed",likely_consequence:"構成概念の過小または過大評価の可能性"},proxy_risk:{status:"moderate",rationale:"自己申告頻度はhabitual physical activityの代理指標である。"},population_transport:{status:"unknown",rationale:"他集団・他文脈への妥当性情報がない。"},missing_information:["reference_standard","validation study","temporal stability"],assumptions:["報告された操作化を正確と仮定する"],overall_assessment:"測定は部分的な構成概念を捉える可能性があるが、妥当性はunknownのまま残す。"};
const statuses=["supported","partially-supported","threatened","not-applicable","unknown","not-assessable"]; for(const x of Object.values(output.assessment)) if(!statuses.includes(x.status)||!x.rationale) throw new Error("invalid assessment"); if(output.construct.stated===output.operationalization.stated) throw new Error("construct and measurement collapsed"); if(!output.missing_information.length||!output.overall_assessment) throw new Error("missing unknown handling"); console.log("assess-measurement-validity execution passed");
