import { readFile, writeFile, readdir } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),".."); const out=path.join(root,"dist"); const coverage=JSON.parse(await readFile(path.join(out,"generated/concept-coverage.json"),"utf8")); const priority={epistemology:["evidence","uncertainty","inference","assumption","underdetermination"],measurement:["construct","operationalization","validity","reliability","measurement-error","proxy","reference-standard","measurement-invariance"],statistics:["sampling-distributions","point-estimation","likelihood-and-mle","standard-error","confidence-interval","data-generating-process","multiple-regression-and-glm","model-misspecification"],"causal-inference":["causal-effect","counterfactual","confounding","collider","mediator","dag","identification","selection-bias"],"research-design":["research-question","estimand","target-population","randomization","observational-study"],metascience:["reproducibility","replicability","publication-bias","selective-reporting","researcher-degrees-of-freedom"]}; coverage.priority_concepts=Object.fromEntries(Object.entries(priority).map(([d,ids])=>[d,ids])); coverage.content_status={mapped:coverage.total_concepts, draft:0, deepened:0}; await writeFile(path.join(out,"generated/concept-coverage.json"),JSON.stringify(coverage,null,2),"utf8"); for(const domain of Object.keys(priority)){const dir=path.join(out,"concepts",domain); let names=[]; try{names=await readdir(dir)}catch{} for(const name of names){const f=path.join(dir,name,"index.html"); try{let h=await readFile(f,"utf8"); h=h.replace(/Status: <strong>DEEPENED<\/strong>/g,`Priority: ${priority[domain].includes(name)}<\/p><p>Content status: MAPPED`); await writeFile(f,h,"utf8")}catch{}}} console.log("concept priority and content status separated");
