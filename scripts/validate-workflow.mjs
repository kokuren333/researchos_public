import { readFile } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),".."); const workflow=JSON.parse(await readFile(path.join(root,"workflows/appraise-observational-study/workflow.yaml"),"utf8")); const skills=new Set(["assess-measurement-validity","identify-confounding","choose-statistical-model"]); if(workflow.steps.length!==3)throw new Error("workflow must have three ordered steps"); for(const step of workflow.steps){if(!skills.has(step.skill))throw new Error(`unknown workflow skill: ${step.skill}`); if(!step.inputs||!Object.values(step.inputs).every(Boolean))throw new Error(`invalid input mapping: ${step.id}`);} if(workflow.steps[2].inputs.measurement_information!=="measurement.output"||workflow.steps[2].inputs.causal_information!=="confounding.output")throw new Error("step output mapping is incomplete"); if(!workflow.final_artifact.filename||!workflow.final_artifact.sections.length)throw new Error("final artifact contract is incomplete"); console.log("appraise-observational-study workflow passed");
