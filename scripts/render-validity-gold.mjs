import { readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const out=path.join(root,"dist/concepts/measurement/validity/index.html");const c=JSON.parse(await readFile(path.join(root,"domains/priority-validity.json"),"utf8"));const graph=JSON.parse(await readFile(path.join(root,"dist/generated/concept-skill-graph.json"),"utf8"));const e=s=>String(s).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;");const sections=["definition","intuition","why","example","misuse","limits","practice"];const body=`<h1>${e(c.title)}</h1>`+sections.map(k=>`<h2>${k}</h2><p>${e(c[k])}</p>`).join("")+`<h2>前提Concept</h2><p>${c.prerequisites.map(e).join("、")}</p><h2>関連Concept</h2><p>${c.related.map(e).join("、")}</p><h2>Used by Skills</h2><p>${(graph.concept_to_skills[c.id]??[]).map(e).join("、")}</p><h2>Sources / Evidence</h2><p><a href="${c.source}">${c.source}</a> — ${e(c.locator)}</p>`;await writeFile(out,`<!doctype html><html lang="ja"><head><meta charset="utf-8"><title>${e(c.title)}</title><style>body{font:16px system-ui;max-width:760px;margin:auto;padding:2rem;line-height:1.8}.lead{font-size:1.2rem}</style></head><body>${body}</body></html>`);console.log("rendered validity gold content");
