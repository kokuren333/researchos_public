import { readFile } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),".."); const input=JSON.parse(await readFile(path.join(root,"tests/fixtures/identify-confounding.valid.json"),"utf8")); const required=["research_question","exposure","outcome","candidate_variables","study_context"]; const missing=required.filter(k=>!input[k]); if(missing.length||!Array.isArray(input.candidate_variables)) throw new Error(`invalid input: ${missing.join(",")}`); const output={variables:input.candidate_variables.map(variable=>({variable,classification:"unknown",rationale:"因果構造の情報が不足しているため分類を確定しない。",evidence_basis:"入力された研究文脈のみ",uncertainty:"high"})),missing_information:["時間順序","未測定の共通原因","因果構造"],assumptions:["入力された研究文脈を正確と仮定する"],overall_assessment:"提示情報だけでは候補変数の分類を確定できない。"}; if(!output.missing_information.length||!output.overall_assessment) throw new Error("unknown handling incomplete"); console.log("identify-confounding valid fixture passed");
